spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: cart-service
      profile: dev
      enabled: false
      import-check:
        enabled: false
  config:
    import: optional:configserver:http://localhost:8888
